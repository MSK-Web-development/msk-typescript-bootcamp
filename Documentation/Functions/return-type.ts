/*
?  Return type annotation.
 - Return type annotations appear after the parameter list.
 - TypeScript can also infer the function’s return type based on its return statements.
*/

function sayHello(): string {
    return "Hello";
}
